import type { BASE_OBJECT_RESPONSE } from '@/types'

export type DELIVERY_FEE_RESPONSE = Nullable<{
  id: number
  created_at: string
  name: string
  price: number
  status: number
  updated_at: string
}>
